#include <unistd.h>
#include<ctype.h>
#include "main.h"
/**
 * mul - returns product of two numbers
 * @a: first operand
 * @b: second operand
 * Return: the product of the two operand
 */

int mul(int a, int b)
{
return (a * b);
}
