#include <unistd.h>
#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: string to be calculated
 */

int _strlen(char *s)
{
return (size_t strlen(s));
}
