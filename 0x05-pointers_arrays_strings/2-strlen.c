#include <unistd.h>
#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: string to be calculated
 * Return: lenght of string
 */

int _strlen(char *s)
{
return (strlen(s));
}
