#include <unistd.h>
#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 * rev_string - reverses a string
 * @s: string to be reversed
 * Return: string in reverse
 */

void rev_string(char *s)
{
return (strrev(s);
}
