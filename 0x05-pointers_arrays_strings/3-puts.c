#include <unistd.h>
#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 * _puts - prints a string, followed by a newline
 * @str: string to be printed
 */

void _puts(char *str)
{
puts(str);
}
