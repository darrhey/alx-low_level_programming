#include "main.h"
/**
 * _print_rev_recursion - prints a string in revcerse
 * @s: string to print in reverse
 * Return: nothing
 */

void _print_rev_recursion(char *s)
{
if (!*s)
{
return;
}
_print_rev_recursion((s + 1));
_putchar(*s);
}
